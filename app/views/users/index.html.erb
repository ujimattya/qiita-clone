<h3>全てのユーザー</h3>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
      <p><%= user.profile %></p> 
    </li>
  <% end %>
  
</ul>

<%= will_paginate %>